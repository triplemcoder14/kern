apiVersion: v2
name: kern
description: Kernel-native Kubernetes observability — operator, agent, and declarative alert rules
type: application
version: 0.1.0
appVersion: "0.6.0"
kubeVersion: ">=1.26.0-0"
